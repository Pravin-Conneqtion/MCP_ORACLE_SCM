steps:
  - label: "Build Wheel :python:"
    commands:
      - cd mcp_oracle_scm
      - uv build

  - wait
  
  - label: "Publish Artifacts to Artifactory :artifactory:"
    env:
      RELEASE_BRANCH: "main"
    commands:
      - .buildkite/scripts/publish_pypi.sh mcp_oracle_scm
